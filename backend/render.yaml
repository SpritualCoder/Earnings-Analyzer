services:
  - type: web
    name: research-portal-backend
    runtime: python
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
```

---

**Steps to deploy backend on Render:**

1. Push your code to GitHub
2. Go to [render.com](https://render.com) → New → Web Service
3. Connect your GitHub repo
4. Set root directory to `backend`
5. Render will auto-detect `render.yaml`
6. Go to Environment → add `GEMINI_API_KEY` with your actual key
7. Click Deploy

Once deployed you'll get a URL like:
```
https://research-portal-backend.onrender.com
```

---

## Frontend on Vercel

**Steps:**

1. Go to [vercel.com](https://vercel.com) → New Project
2. Connect your GitHub repo
3. Set root directory to `frontend`
4. Add environment variable:
```
NEXT_PUBLIC_API_URL = https://research-portal-backend.onrender.com